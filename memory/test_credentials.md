# Current access context

- No login accounts or credentials were created or changed in this task.
- The preview opens without signing in and displays a clearly labeled sample workspace.
- Department sample changes use localStorage key `churchflow-department-workspaces-v1`.
- Real shared department APIs remain administrator-protected through the existing OAuth/tRPC authentication.
- No DATABASE_URL or OAuth provider credentials are configured in this environment; do not bypass those restrictions.
- The user explicitly changed the requirement from AI generation to feature briefs for the development team. No AI key is needed or configured.